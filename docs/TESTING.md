# استراتيجية الاختبار (Testing)

## المبدأ
كل مرحلة تُختبر قبل الانتقال للتالية (طلب صريح من المستخدم) — لا نبني ميزة
جديدة فوق أساس غير مختبر.

## أنواع الاختبار

| النوع | الأداة | متى |
|---|---|---|
| Unit | Jest | لكل Service منطق أعمال (حسابات، قواعد صلاحية، RBAC) |
| Integration/e2e | Jest + Supertest + قاعدة بيانات اختبار حقيقية | لكل Module جديد (auth, iam, integrations) |
| DB constraints | اختبار مباشر عبر Prisma على قاعدة اختبار | RLS policies، Unique constraints |

## اختبارات المرحلة 1 (الحد الأدنى الإلزامي)
- تسجيل منشأة جديدة ينشئ Company + Branch افتراضي + Warehouse افتراضي +
  User + Membership واحدة بدور Owner بكل الصلاحيات.
- تسجيل الدخول الناجح/الفاشل (كلمة مرور خاطئة، مستخدم غير موجود، حساب معطّل).
- Refresh token: تجديد ناجح، رفض توكن منتهٍ/مُبطَل، إبطال السلسلة عند إعادة
  استخدام توكن مُستهلَك، واستمرار العمل بعد اختيار/تبديل المنشأة.
- RBAC: مستخدم بدون صلاحية `X` يُرفض بـ403 عند محاولة الوصول لمسار يتطلبها.
- **عزل المستأجرين**: مستخدم من منشأة A لا يستطيع قراءة/تعديل أي سجل من منشأة
  B، حتى بمعرفة الـID مباشرة (اختبار صريح لهذه الحالة تحديدًا، بما فيه فحص
  RLS مباشر على `memberships`).
- Audit log: عملية حساسة (مثل تغيير دور مستخدم) تُنتج سجل Audit صحيح.
- Integration skeleton: إنشاء/قراءة `integration_connections` بحالاته دون أي
  استدعاء فعلي لخدمة خارجية (لأنه لا يوجد Adapter بعد).

## اختبارات نموذج الهوية متعدد المنشآت (Membership) — إلزامية بعد Auth/IAM refactor
راجع `docs/DOMAIN_MODEL.md` للنموذج الكامل. الحد الأدنى المُختبَر فعليًا في
`backend/test/app.e2e-spec.ts`:
1. مستخدم بعضوية واحدة فقط: تسجيل الدخول يمنح tokens مباشرة، بدون شاشة اختيار.
2. مستخدم بعضويتين (نفس البريد مُرفَق بمنشأتين عبر `IamService.createUser`):
   تسجيل الدخول يُعيد `tenantSelectionRequired` بدل tokens.
3. اختيار منشأة يملك المستخدم عضوية نشطة فيها عبر `/auth/select-tenant` ينجح.
4. اختيار منشأة لا يملك المستخدم أي عضوية فيها (Tenant C) عبر
   `/auth/select-tenant` أو `/auth/switch-tenant` يُرفض بـ403 — يثبت أن
   الخادم لا يثق بـ`companyId` القادم من العميل، بل يتحقق من Membership
   فعليًا في كل مرة.
5. نفس الشخص بدور مختلف تمامًا في كل منشأة (مثلًا Owner في A، Accountant في
   B) — يُتحقق من أن `roles`/`permissions` في `/auth/me` تختلف فعليًا بين
   الجلستين.
6. عضوية `suspended` تفقد كل وصول فورًا (حتى لمسارات بلا `@RequirePermissions`)
   عبر `MembershipGuard`، رغم أن الـaccess token ما زال صالحًا فنيًا.
7. إرفاق مستخدم موجود بمنشأة يملك فيها عضوية بالفعل → `409 Conflict`.
8. إرفاق مستخدم موجود لا يُغيّر كلمة مروره أبدًا، حتى لو أُرسلت كلمة مرور في
   الطلب — يُتحقق بتسجيل الدخول لاحقًا بكلمة المرور الأصلية.
9. RBAC/tenant-isolation tests السابقة (البند أعلاه) تستمر بالنجاح كاملة بعد
   إعادة الهيكلة — لا تراجع.

## اختبارات المرحلة 2 (Products/Inventory/Customers/Suppliers) — إلزامية
راجع `backend/test/phase2.e2e-spec.ts` (33 اختبارًا إجماليًا مع اختبارات
المرحلة 1). الحد الأدنى المُختبَر فعليًا:

**المنتجات**: دورة CRUD + Audit كاملة (بما فيها أحداث `price_change`/
`sku_change` المنفصلة)، بحث بالاسم/SKU/الباركود، `SKU` وباركود فريدان لكل
منشأة وليس عالميًا (نفس القيمة تُقبَل لمنشأة أخرى)، رفض ربط منتج
بتصنيف/علامة/وحدة تابعة لمنشأة أخرى، رفض قراءة/تعديل/حذف منتج من منشأة أخرى
حتى بمعرفة الـID، رفض الوصول بلا صلاحية.

**المخزون**: رصيد افتتاحي + تسوية + رفض تسوية تُنتج رصيدًا سالبًا، تحويل
فعلي بين مستودعين، **اختبار تزامن بـ10 طلبات HTTP متزامنة حقيقية** على نفس
`(warehouse, product)` يتحقق أن الرصيد النهائي = 10 بالضبط وعدد حركات
`stock_movements` الناتجة = 10 بالضبط، دورة جرد كاملة (إنشاء → تحديث سطور →
اعتماد → التحقق من تحديث الأرصدة تلقائيًا → التحقق من تجميد السطور بعد
الاعتماد)، رفض العمل على مستودع تابع لمنشأة أخرى، رفض الوصول بلا صلاحية.

**العملاء والموردون**: دورة كاملة، الجوال غير فريد عالميًا (يُقبَل تكراره)،
رفض قراءة/تعديل عميل أو مورد من منشأة أخرى، `reference` فريد لكل منشأة إن
وُجد، رفض الوصول بلا صلاحية.

**RLS مباشر**: استعلام Prisma مباشر (بدون طبقة الخدمة) على `products`/
`customers` يتحقق أن RLS وحدها (بلا أي فحص تطبيقي) تمنع القراءة عبر المنشآت.

## اختبارات المرحلة 2.1 (نطاق الفروع/المستودعات) — إلزامية
راجع `backend/test/phase2.e2e-spec.ts` قسم "نطاق الفروع/المستودعات" (33
اختبار المرحلة 2 + 5 اختبارات جديدة = 38 إجمالًا). يغطي مصفوفة الاختبار
المطلوبة كاملة:

- **نفس المنشأة / فرع مُصرَّح** → PASS: عضو بإسناد دور مقيّد بفرع الوحدة
  الافتراضي ينجح في رصيد افتتاحي على مستودع ذلك الفرع.
- **نفس المنشأة / فرع غير مُصرَّح** → FAIL (403): نفس العضو يُرفض عند تسوية،
  تحويل (أي من الطرفين)، أو إنشاء جرد على مستودع تابع لفرع آخر بنفس المنشأة.
- **نطاق الفرع يشمل كل مستودعاته**: عضو مُصرَّح لفرع كامل (لا لمستودع واحد)
  ينجح على أكثر من مستودع تابعين لذلك الفرع.
- **إسنادا دور (فرعان)**: عضو بإسنادَي `MembershipRole` بفرعين مختلفين ينجح
  على مستودعات كلا الفرعين معًا (اتحاد النطاقات).
- **نطاق كامل تلقائي لفرع جديد**: عضو Owner (`branchId: null` عند التسجيل)
  ينجح فورًا على فرع/مستودع أُنشئا بعد إسناد دوره، بلا أي إسناد إضافي.
- **عضوية معلَّقة** → FAIL (403 عبر `MembershipGuard`، حتى لمستودع ضمن نطاقها).
- **مختلف المنشأة / مستودع** → FAIL (404 — سلامة مرجعية عبر المنشآت، غير
  متأثرة بمنطق النطاق الجديد).
- **صلاحية سارية + نطاق فرع غير سارٍ** → FAIL (403 — الصلاحية وحدها لا تكفي).
- **قوائم القراءة لا ترفض، تُصفّي**: طلب `warehouseId` خارج النطاق على
  `GET /inventory/stock-levels` يُرجع 200 بنتيجة فارغة، لا 403.
- **سيناريو متعدد المستأجرين الكامل**: منشأتان (A بفرعين A1/A2، B بفرع B1)،
  أربعة مستخدمين بنطاقات مختلفة (A1 فقط، A1+A2 معًا، B1 عبر Owner B)، يتحقق
  أن كل تركيبة وصول/رفض تطابق النموذج المطلوب بالضبط.
- **بلا تراجع**: كل اختبارات المرحلة 2 الـ33 (بما فيها اختبار التزامن
  بـ10 طلبات) تستمر بالنجاح كاملة بعد هذا التغيير.

## اختبارات المرحلة 3 (POS/Sales/Payments/Invoices) — إلزامية
راجع `backend/test/phase3.e2e-spec.ts` (25 اختبارًا جديدًا؛ 38 اختبار سابق +
25 = 63 إجمالًا). يغطي مصفوفة الاختبار المطلوبة كاملة:

**التفويض**: بلا توكن → 401. عملية بيع من منشأة أخرى غير مرئية حتى بمعرفة
الـID → 404. مستودع من منشأة أخرى تمامًا → 404. عضو مقيّد بفرع يُرفض بيع من
مستودع فرع آخر بنفس المنشأة → 403. جهاز POS تابع لفرع مختلف عن المستودع →
409. جهاز POS غير نشط → 409؛ جهاز من منشأة أخرى → 404. عضوية معلَّقة تُرفض
حتى مع صلاحية `sales.create` سارية → 403. كاشير بلا `sales.cancel` → 403 عند
محاولة الإلغاء.

**الوظيفي**: بيع سطر واحد يحسب الإجمالي/الضريبة بدقة، يخصم المخزون، يولّد
فاتورة، ويُسجَّل Audit. بيع بعدة سطور مع خصم على سطر يحسب الإجماليات الجزئية
والكلية بشكل صحيح. Split payment (نقدي + بطاقة) يُقبَل حين يتطابق المجموع مع
الإجمالي بالضبط، ويُرفض بـ400 حين لا يتطابق (حالة "فشل دفع"). خصم أكبر من
قيمة السطر → 400. مخزون غير كافٍ → 409 بلا إنشاء أي سجل بيع. منتج معطّل لا
يمكن بيعه. بيع بعميل وبيع بلا عميل (نقدي سريع) كلاهما يعمل. إلغاء بيع مكتمل
يُرجع المخزون ويُعلّم الفاتورة كملغاة؛ إلغاء مرتين → 409. حالة الدفع `pending`
مدعومة في نموذج البيانات (تحقُّق مباشر عبر Prisma، بلا تدفق HTTP وهمي —
لا مزوّد خارجي فعلي بعد).

**التزامن وIdempotency**: طلب مكرر بنفس `clientReferenceId` (تسلسليًا، ثم 5
طلبات متزامنة حقيقية) يُنتج سجل بيع واحد فقط دائمًا. **عمليتا بيع متزامنتان
حقيقيتان** تحاولان بيع كمية تتجاوز المتاح مجتمعتين (Stock=10، كل طلب يبيع 7)
— واحدة فقط تنجح (201)، الأخرى تُرفض (409)، الرصيد النهائي = 3 بالضبط، ولا
يوجد أي احتمال لرصيد سالب. 10 طلبات بيع متزامنة حقيقية تُنتج 10 أرقام فاتورة
فريدة بلا أي تكرار.

**القوائم والصلاحيات**: عضو بلا `sales.create` → 403. قائمة المبيعات/الفواتير
تعرض ما يخص المنشأة فقط، مع صفحات.

**RLS مباشر**: استعلام Prisma مباشر (بدون طبقة الخدمة) على `sales`/`invoices`
يتحقق أن RLS وحدها تمنع القراءة عبر المنشآت.

**Browser test (Playwright)**: تدفق كامل حقيقي عبر متصفح فعلي (تسجيل دخول →
فتح POS → بحث عن منتج → إضافة للسلة → تعديل كمية → اختيار عميل → دفع نقدي →
إتمام البيع → التحقق من ظهور رقم الفاتورة في الواجهة ومطابقته عبر الـAPI →
التحقق من تحديث المخزون عبر الـAPI) + سيناريو فشل واحد (مخزون غير كافٍ يعرض
رسالة خطأ واضحة في الواجهة ولا يُغيّر الرصيد). نُفِّذ يدويًا بتشغيل فعلي
متزامن للـbackend/frontend، بلا أخطاء console حقيقية (السجل الوحيد الملاحظ
هو تسجيل طلب الشبكة 409 المتوقَّع لسيناريو الفشل نفسه).

**بلا تراجع**: كل اختبارات المراحل 1/2/2.1 الـ38 تستمر بالنجاح كاملة.

## اختبارات المرحلة 4 (Purchases/Expenses/Accounting Foundation) — إلزامية
راجع `backend/test/phase4.e2e-spec.ts` (28 اختبارًا جديدًا؛ 63 اختبار سابق
+ 28 = **91 إجمالًا**). يغطي مصفوفة الاختبار المطلوبة كاملة عبر 6 مجموعات:

**دليل الحسابات (Chart of Accounts)**: تسجيل منشأة جديدة ينشئ دليل حسابات
افتراضيًا كاملًا (كل الحسابات الموثَّقة في `docs/CHART_OF_ACCOUNTS.md`).
إنشاء حساب جديد يتطلب `accounting.manage`، وتعديله (`name`/`isActive`)
يعمل. رمز حساب مكرر لنفس المنشأة → `409`. **لا Endpoint لإنشاء أو تعديل
قيد محاسبي يدويًا** (منع الإدخال المزدوج — تحقُّق مباشر أن السطح غير
موجود، لا مجرد صلاحية مرفوضة). حساب من منشأة أخرى → `404` عند التعديل.

**سلامة القيد المحاسبي (Double-Entry Integrity)**: قيد متوازن (استدعاء
`JournalService` مباشرة) ينجح. قيد غير متوازن (مدين ≠ دائن) → يُرفض. قيد
بقيمة صفرية → يُرفض. عكس قيد مُعكوس بالفعل → يُرفض (409).

**المشتريات (Purchases)**: إنشاء أمر شراء ثم استلامه يزيد المخزون ويولّد
قيدًا متوازنًا (مخزون + ضريبة مدخلات = ذمم دائنة). مستودع/مورد من منشأة
أخرى → `404`. استلام مرتين → `409` (لا يُستلَم المخزون مرتين). **استلام
متزامن حقيقي** لنفس أمر الشراء: مرة واحدة فقط تنجح، المخزون لا يتضاعف. 10
أوامر شراء من طلبات متزامنة حقيقية → 10 أرقام مرجعية فريدة بلا تكرار.
طلب مكرر بنفس `clientReferenceId` → نفس أمر الشراء دون تكرار. إلغاء أمر
لم يُستلَم يعمل؛ إلغاء أمر مُستلَم → `409`. عضو مقيّد بفرع لا يمكنه الشراء
لمستودع فرع آخر بنفس المنشأة → `403`. عضو بلا `purchases.create` → `403`.

**المصروفات (Expenses)**: إنشاء مصروف ينشئ قيدًا متوازنًا (مصروف =
نقدية)، ويُسجَّل Audit. تعديل مبلغ مصروف يعكس القيد القديم وينشئ قيدًا
جديدًا صحيحًا. حذف مصروف يعكس القيد؛ حذف/تعديل مصروف محذوف بالفعل → `409`
في كلتا الحالتين. مصروف مرتبط بفرع خارج نطاق العضوية → `403`. عضو بلا
`expenses.create` → `403`. مصروف من منشأة أخرى غير مرئي حتى بمعرفة الـID
→ `404`. 5 طلبات متزامنة حقيقية بنفس `clientReferenceId` → مصروف واحد فقط.

**التكامل مع المحاسبة (Sale/Purchase/Expense → Accounting)**: بيع مكتمل
ينتج قيدًا متوازنًا؛ إلغاؤه ينتج قيدًا عكسيًا ويُعلّم الأصلي `reversed`.

**RLS مباشرة على جداول المرحلة الرابعة**: استعلام Prisma مباشر (بدون طبقة
الخدمة) على `purchases`/`expenses`/`accounts`/`journal_entries` يتحقق أن
RLS وحدها (بلا أي فحص تطبيقي) تمنع القراءة عبر المنشآت.

**Browser test (Playwright)**: تدفق يدوي حقيقي عبر متصفح فعلي (تسجيل دخول
→ المشتريات: إنشاء أمر شراء ثم استلامه، التحقق من زيادة المخزون عبر الـAPI
→ المصروفات: تسجيل مصروف، التحقق من توازن قيده عبر الـAPI → المحاسبة:
دليل الحسابات ظاهر في تبويب "دليل الحسابات"، نافذة تفاصيل قيد محاسبي تُظهر
مدين = دائن في تبويب "القيود المحاسبية"). سكربت مؤقت غير مُدرَج في
المستودع (نفس اتفاقية Playwright اليدوية من المرحلة 3) — نجحت كل الخطوات
بلا أخطاء console حقيقية.

**بلا تراجع**: كل اختبارات المراحل 1/2/2.1/3 الـ63 تستمر بالنجاح كاملة.

## اختبارات Milestone 1: Accounting Completion — إلزامية
راجع `backend/test/milestone1.e2e-spec.ts` (21 اختبارًا جديدًا؛ 91
اختبار سابق + 21 = **112 إجمالًا**، مُتحقَّق فعليًا بتشغيل
`npx jest --config ./test/jest-e2e.json --runInBand` — راجع "بلا تراجع"
أدناه لكيفية إعادة التحقق). يغطي مصفوفة الاختبار كاملة عبر 6 مجموعات:

**التقارير المالية (Trial Balance / General Ledger / P&L / Balance
Sheet)**: ميزان مراجعة متوازن (مدين = دائن) بعد بيع ومصروف، ورصيد صافٍ
صحيح لحساب النقدية. نطاق التاريخ (`dateFrom`/`dateTo`) يُقصي القيود
خارج المدى. دفتر أستاذ حساب معيّن برصيد جارٍ صحيح. صافي الربح = الإيرادات
− المصروفات، ويظهر كبند "أرباح مرحّلة غير مقفلة" (`computed: true`)
متوازن في الميزانية العمومية. حساب من منشأة أخرى في دفتر الأستاذ → `404`
(IDOR). عضو بلا `accounting.reports.view` → `403` على كل التقارير
الأربعة.

**الذمم المدينة/الدائنة (AR/AP Subledger)**: AR يعود فارغًا هيكليًا حتى
بعد بيع مرتبط بعميل — يثبت القيد الموثَّق (لا بيع آجل في النظام)، وليس
خطأ. AP يعرض رصيدًا صحيحًا لكل مورد بعد استلام مشتريات + كشف حساب
مفصَّل. كشف حساب مورد من منشأة أخرى → `404` (IDOR). عضو بلا
`accounting.ar.view`/`ap.view` → `403`.

**الأرصدة الافتتاحية (Opening Balances)**: تسجيل رصيد متوازن ينجح، ولا
يمكن تسجيل آخر قبل عكسه (`409`). رصيد غير متوازن → `422`. عكس الرصيد
يسمح بتسجيل رصيد جديد؛ عكسه بلا رصيد نشط → `404`. **طلبان متزامنان
حقيقيان** لتسجيل رصيد افتتاحي — واحد فقط ينجح (`201`)، الآخر `409`
(الفهرس الفريد الجزئي على مستوى Postgres، لا فحص تطبيقي فقط)، والتحقق
المباشر عبر Prisma أن صفًا واحدًا فقط `posted` موجود فعليًا. عضو بلا
`accounting.opening_balance.manage` → `403`.

**الفترات المحاسبية (Fiscal Periods)**: إنشاء فترة، وفترة متقاطعة معها
→ `409`، وتاريخ بداية بعد النهاية → `400`. إقفال الفترة الحالية يمنع
ترحيل قيود جديدة (مصروف جديد → `409`)؛ إعادة الفتح تسمح مجددًا. إقفال
فترة مُقفلة بالفعل → `409`؛ إعادة فتح فترة مفتوحة بالفعل → `409`. فترة
محاسبية من منشأة أخرى → `404` عند محاولة إقفالها (IDOR). عضو بلا
`accounting.period.manage` → `403`.

**نطاق الفروع في التقارير (Branch scope)**: محاسب مقيّد بفرع واحد لا
يرى في دفتر الأستاذ إلا حركات فرعه (سطر واحد)، بينما Owner غير المقيَّد
يرى حركات كل الفروع (سطرين) على نفس الحساب — نفس بيانات الاختبار
بالضبط، فرق واحد فقط هو نطاق العضوية المستعلِمة.

**بلا تراجع**: كل اختبارات المراحل 1/2/2.1/3/4 الـ91 تستمر بالنجاح
كاملة، مع تصحيح واحد داخل `journal.service.ts` (ترتيب تعليم القيد
الأصلي `reversed` قبل إنشاء قيد العكس، راجع `docs/ACCOUNTING.md` "حارس
التزامن") — لم يُغيّر أي سلوك أو استجابة API ملحوظة لأي اختبار سابق.

**Browser test (Playwright)**: تدفق يدوي حقيقي عبر متصفح فعلي (تسجيل
منشأة → زرع بيع/مصروف/شراء عبر الـAPI → صفحة التقارير: زيارة التبويبات
الأربعة والتحقق من عرض بيانات حقيقية → صفحة الذمم: تبويبا AR/AP → تبويب
الأرصدة الافتتاحية في `/accounting`: إنشاء + عرض → تبويب الفترات
المحاسبية: إنشاء + عرض) — كل الخطوات نجحت بلا أخطاء console. عِلّة
تنسيق فعلية اكتُشفت وصُحِّحت أثناء هذا الاختبار: الأرقام المالية كانت
تُعرَض بـ`toLocaleString('ar-SA', ...)` (أرقام هندية شرقية، مثل ٢٠٫٠٠)
بدل الأرقام الغربية المستخدمة في كل صفحة أخرى بالتطبيق — صُحِّحت إلى
`.toFixed(2)` في `ReportsPage.tsx` و`ReceivablesPayablesPage.tsx`.

## اختبارات Milestone 2: Production Hardening + Demo/Staging Readiness

لا منطق أعمال جديد في هذا الـMilestone (CORS/Logging/Health/Docker/CI
بنية تحتية، لا Endpoints جديدة) — لذا لا مجموعة `milestone2.e2e-spec.ts`
جديدة. التغيير الوحيد على مجموعة الاختبارات الخلفية الحالية: تصحيح واحد
في `test/app.e2e-spec.ts` (اختبار "لا يُرجع passwordHash أبدًا في
استجابات المستخدمين") ليقرأ `res.body.data` بدل `res.body` مباشرة، بعد
أن أصبح `GET /iam/users` مُرقَّمًا (`{data, meta}`) بدل مصفوفة مسطّحة —
راجع `docs/API.md`/`docs/PROJECT_STATUS.md` قسم Milestone 2. **112/112
لا تزال تنجح بالكامل**، مُتحقَّق فعليًا بتشغيل
`npx jest --config ./test/jest-e2e.json --runInBand`.

### اختبارات الواجهة الأمامية الآلية (Vitest) — جديدة في هذا الـMilestone

قبل هذا الـMilestone، الواجهة الأمامية لم تملك أي اختبار آلي على
الإطلاق — كل تحقق كان Playwright يدويًا فقط (راجع أقسام "Browser test"
أعلاه). الآن `vitest` + `@testing-library/react` +
`@testing-library/user-event` مُهيّأة عبر `frontend/vite.config.ts`
(قسم `test`، `environment: 'jsdom'`) و`frontend/src/test/setup.ts`
(`@testing-library/jest-dom/vitest`).

**7 اختبارات في 3 ملفات**، كلها تحت `frontend/src/pages/__tests__/`،
**مُتحقَّقة فعليًا بتشغيل `npx vitest run` (7/7 ناجحة)**:
- `LoginPage.test.tsx` (3 اختبارات): إرسال identifier/password والتنقل
  عند نجاح دخول عضوية واحدة، ومسارات أخرى ذات صلة بشاشة الدخول.
- `RegisterPage.test.tsx` (2 اختبارًا): إرسال بيانات المنشأة/المالك
  وإتمام التسجيل عند النجاح.
- `DashboardPage.test.tsx` (2 اختبارًا): جلب وعرض البطاقات (tiles) التي
  يملك العضو صلاحيتها فقط — يثبت مباشرة إصلاح فجوة RBAC الموصوفة أدناه.

تشغيل محلي: `cd frontend && npm run test` (مرة واحدة) أو
`npm run test:watch` (مراقبة).

### RBAC وحالات التحميل/الخطأ في الواجهة الأمامية — إصلاحات هذا الـMilestone

اكتُشف بتدقيق مخصص قرأ كل صفحات الواجهة الأمامية الـ16:
- `DashboardPage.tsx` لم تكن تُطبِّق أي بوابة صلاحية على الإطلاق، وكانت
  تجلب كل البطاقات عبر `Promise.all` (فشل جلب واحد يُفرغ اللوحة بالكامل).
  الآن كل بطاقة مُقيَّدة بصلاحيتها الخاصة وتُجلَب عبر `Promise.allSettled`
  (فشل جزئي يعرض البطاقات التي نجحت فقط، لا شاشة فارغة) — مُختبَر آليًا
  في `DashboardPage.test.tsx` أعلاه.
- `InvoicesPage.tsx` لم تكن تتحقق من `hasPermission` إطلاقًا — الآن
  مُقيَّدة بـ`invoices.read`.
- 9+ صفحات كان جلب التحميل الأساسي (mount fetch) فيها بلا `try/catch` أو
  مؤشر تحميل (أزرار الإنشاء/التعديل/الحذف فقط كانت مُجهَّزة سابقًا):
  `ProductsPage.tsx`, `PartyPage.tsx` (العملاء/الموردون),
  `CatalogPage.tsx`, `InventoryPage.tsx`, `PurchasesPage.tsx`,
  `ExpensesPage.tsx`, `AccountingPage.tsx` (تبويبا الحسابات/القيود
  تحديدًا)، `PosPage.tsx` (جلب البدء + بحث المنتج)، `ReportsPage.tsx`
  (قائمة الحسابات المنسدلة في تبويب دفتر الأستاذ)، `InvoicesPage.tsx`.
  الآن كل واحدة تستخدم زوج حالة `listLoading`/`listError` (أو اسم حالة
  موجود مكافئ)، تعرض "...جارٍ التحميل" أثناء الجلب و`ErrorBanner` عند
  الفشل.
- **معالجة انتهاء الجلسة (401)**: `SESSION_EXPIRED_EVENT`
  (`frontend/src/api/client.ts`) يُطلَق عبر `window.dispatchEvent` عندما
  يفشل تجديد التوكن (الجلسة منتهية فعليًا من طرف الخادم).
  `AuthProvider` (`frontend/src/state/auth.tsx`) يستمع له الآن ويُفرغ
  `me`، فيعيد `RequireAuth` (`App.tsx`) التوجيه إلى `/login` بدل ترك
  شاشة مُصادَق عليها باليات تفشل فيها كل الطلبات التالية صامتة.
- **حد معروف موثَّق، وليس مكتملًا**: `403` (Forbidden) لا يزال يُعرَض
  كرسالة خطأ عامة عبر حالة الخطأ الخاصة بكل صفحة — لا واجهة "غير مخوَّل"
  مخصصة ومنفصلة عن أي خطأ آخر أُضيفت.

## اختبارات Milestone 3: Excel Import — إلزامية

راجع `backend/test/imports.e2e-spec.ts` (**22 اختبارًا جديدًا؛ 112 اختبار
سابق + 22 = 134 إجماليًا**، مُتحقَّق فعليًا بتشغيل `npm run test:e2e`
الكامل، **134/134 ناجحة، صفر تراجع**). يغطي:

- **المسار الكامل**: Upload → Detect (اقتراح ربط تلقائي صحيح) → Map →
  Preview (بلا أي كتابة — يُتحقَّق صراحة أن `GET /products` لا يُظهر
  المنتج قبل Confirm) → Validate → Confirm (يكتب فعليًا، يُتحقَّق من
  ظهور المنتجات في `GET /products` بعده).
- **رفض قبل Validate**: `confirm` على مهمة لم تُتحقَّق بعد → `409`.
- **صف بحقل مطلوب مفقود**: يُرفَض برسالة محدَّدة ("SKU مطلوب")، ولا يمنع
  استيراد الصف الصحيح الآخر في نفس الملف (استيراد جزئي مُبلَّغ عنه، لا
  فساد صامت).
- **SKU/باركود مكرر** مقابل بيانات موجودة فعلًا، ومقابل تكرار **داخل نفس
  الملف** (رسالة مختلفة توضّح رقم الصف الأول).
- **عملاء/موردون**: استيراد ناجح كامل لكل نوع.
- **رصيد افتتاحي للمخزون**: يستخدم `InventoryService.setOpeningBalance`
  الموجودة أصلًا فعليًا (رصيد حقيقي يظهر في `GET /inventory/stock-levels`
  بعد Confirm)؛ يرفض إنشاء مهمة بلا مستودع مستهدف؛ **عضو مقيَّد بفرع آخر
  يفشل صفه في Confirm برسالة نطاق الفروع نفسها** التي يفرضها
  `InventoryService` أصلًا (لا Bypass).
- **ملف غير صالح**: محتوى ليس ZIP حقيقي → المهمة تُسجَّل `failed` بسبب
  واضح، لا استثناء غير معالَج (500).
- **Idempotency**: نفس `clientReferenceId` عند الرفع يُرجع نفس المهمة؛
  استدعاء `confirm` مرتين لا يستورد الصف مرتين (يُتحقَّق بعدّ نتائج
  `GET /products?search=`).
- **عزل المستأجرين/IDOR**: منشأة أخرى تحاول الوصول لمهمة استيراد منشأة
  أولى عبر كل الـEndpoints (Get/Mapping/Preview/Validate/Confirm) → `404`
  في كل حالة.
- **RBAC**: عضو بلا `import.create` → `403` عند الرفع؛ عضو بلا
  `import.read` → `403` عند عرض السجل.
- **عزل تخزين الملفات**: مفتاح التخزين يطابق
  `imports/<companyId>/<jobId>/source.xlsx` بالضبط؛ لا Endpoint لتنزيل
  ملف خام (`404` على مسار افتراضي).
- **إلغاء**: مهمة جاهزة يمكن إلغاؤها؛ لا يمكن تحقيقها/تأكيدها بعد الإلغاء
  (`409`).

### الواجهة الأمامية

**Vitest**: `ImportPage.test.tsx` جديد (اختباران) — بوابة الصلاحية
(`import.create` غائبة → `ErrorBanner` بدل المعالج)، وتحميل أنواع
البيانات المدعومة + تعطيل زر الرفع قبل اختيار ملف. **9/9 اختبارات Vitest
إجماليًا الآن (7 سابقة + 2)**، مُتحقَّق فعليًا (`npx vitest run`).

**Playwright**: `frontend/e2e/excel-import.spec.ts` جديد — يستخدم ملف
`.xlsx` حقيقي مُلتزَم بالمستودع (`frontend/e2e/fixtures/import-products.xlsx`،
منتجان بأعمدة SKU/الاسم/التكلفة/السعر)، ويمرّ بالتدفق كاملًا عبر الواجهة
الحقيقية: تسجيل منشأة → رفع الملف → مراجعة الربط المُقترَح تلقائيًا →
حفظ → معاينة → تحقق → تأكيد → التحقق أن المنتجَين المستورَدَين يظهران
فعليًا في `/products` وفي سجل عمليات الاستيراد. راجع القسم التالي لعدد
الاختبارات الإجمالي المُتحقَّق منه بالتشغيل الفعلي.

## اختبارات Milestone 4: ZATCA E-Invoicing Readiness (Phase 1) — إلزامية

راجع `backend/test/einvoice.e2e-spec.ts` (**5 اختبارات e2e جديدة**) +
`backend/src/modules/einvoice/tlv-qr.service.spec.ts` (**4 اختبارات
Unit جديدة — أول ملف `.spec.ts` (Unit، لا e2e) في الـBackend على
الإطلاق**، يُشغَّل عبر `npx jest src`، منفصل عن مجموعة e2e). **139/139**
e2e إجماليًا (134 سابقة + 5 جديدة، صفر تراجع) + **4/4** Unit، كلها
مُتحقَّق منها فعليًا بالتشغيل.

يغطي:
- **توليد QR فعلي عند إتمام بيع**: تسجيل منشأة برقم ضريبي حقيقي →
  إتمام بيع → `GET /invoices/:id` يُظهر `compliance.qrCode` → **فك ترميز
  TLV/Base64 الفعلي والتحقق أن القيم الخمس (اسم البائع، الرقم الضريبي،
  الطابع الزمني، الإجمالي، الضريبة) تطابق بيانات الفاتورة الحقيقية
  بالضبط** — ليس فحص "قيمة غير فارغة"، بل تحقق كامل من صحة الترميز.
- **حالة عدم وجود رقم ضريبي**: منشأة بلا رقم ضريبي → سجل امتثال يُنشأ
  (`status: not_submitted`) لكن `qrCode: null` — مُختبَر صراحة، وليس
  افتراضًا.
- **ترميز TLV على مستوى البايت**: طول كل حقل بالبايت (UTF-8) لا بعدد
  الأحرف — **مُختبَر صراحة باسم بائع عربي متعدد البايت** (`متجر البقالة`)
  للتأكد أن الترميز صحيح لغويًا، لا للنصوص الإنجليزية فقط.
- **Audit**: توليد سجل الامتثال يُسجَّل في `audit_logs` فعليًا (`action:
  'einvoice.compliance.generate'`) — مُتحقَّق عبر استعلام Prisma مباشر،
  نفس نمط التحقق من Audit في `phase3.e2e-spec.ts`/`phase4.e2e-spec.ts`.
- **عزل المستأجرين**: منشأة أخرى لا تستطيع رؤية فاتورة (وبالتالي سجل
  الامتثال المرتبط بها) منشأة أولى — `404`.
- **لا فساد للبيانات المحاسبية/المخزون**: تشغيل كامل مجموعة الاختبارات
  السابقة (134 اختبارًا) بعد ربط `EInvoiceService` بـ`SalesService`
  يثبت أن الاستدعاء الإضافي لم يكسر أي مسار محاسبي/مخزون/POS موجود —
  صفر تراجع، وليس ادّعاءً.

### الواجهة الأمامية

**Vitest**: `InvoicesPage.test.tsx` جديد (اختباران) — بوابة الصلاحية،
وأن زر "عرض QR" يظهر **فقط** للفواتير التي تملك `compliance.qrCode`
فعليًا (لا زر لفاتورة بلا رمز) وأن النقر عليه يعرض الصورة الحقيقية.
**11/11** اختبارات Vitest إجماليًا الآن (9 سابقة + 2)، مُتحقَّق فعليًا.

**Playwright**: `frontend/e2e/einvoice-qr.spec.ts` جديد — **يثبت أن ميزة
QR قابلة للوصول فعليًا من مستخدم حقيقي عبر الواجهة**، وليست مدفونة في
الخادم فقط: تسجيل منشأة **مع إدخال رقم ضريبي** (حقل جديد أُضيف لنموذج
`RegisterPage.tsx` في هذا الـMilestone تحديدًا لهذا الغرض — بدونه كانت
الميزة ستبقى غير قابلة للوصول لأي مستخدم حقيقي رغم اكتمالها في الخادم) →
منتج → مخزون → بيع POS → فتح رمز QR من صفحة المبيعات والتحقق أنه صورة
`data:image/png;base64,...` حقيقية قابلة للعرض، لا نص/JSON عشوائي.
**4/4** Playwright إجماليًا الآن (3 سابقة + 1)، نُفِّذت فعليًا ضد الحزمة
الحقيقية الكاملة ونجحت.

### Milestone 5 — Accounting Completion Verification (إغلاق فجوات اختبار فقط)

المراجعة المعمارية لهذا الـMilestone أثبتت أن Trial Balance/GL/P&L/
Balance Sheet/AR/AP/Fiscal Periods/Opening Balances كانت مُنفَّذة ومُختبرة
بالفعل خلفيًا (`test/milestone1.e2e-spec.ts`، 21 اختبارًا). الفجوات
الحقيقية الوحيدة كانت في اختبارات الواجهة الأمامية:

**Backend**: أُضيف تحقق `AuditLog` صريح (`tx.auditLog.findFirst`، نفس نمط
`phase4.e2e-spec.ts`) داخل اختبارَي `accounting.period.close` و
`accounting.opening_balance.create` الموجودَين مسبقًا — **21/21** اختبارًا
في `milestone1.e2e-spec.ts` (نفس العدد، تحققات إضافية داخل اختبارَين
موجودَين لا اختباران جديدان). **139/139** خلفية إجماليًا (بدون تغيير عن
Milestone 4).

**Vitest**: ثلاثة ملفات جديدة لم يكن لها أي اختبار سابقًا —
`ReportsPage.test.tsx` (7، تغطي بوابة الصلاحية + التحميل/الخطأ/الفراغ لكل
تبويب + التبديل بين التبويبات يُطلق طلب API جديدًا)،
`ReceivablesPayablesPage.test.tsx` (5، تغطي تركيبات الصلاحية AR/AP + نص
الفجوة الهيكلية لـAR + فتح كشف حساب مورد)، و`AccountingPage.test.tsx` (7،
تغطي تبويبَي الأرصدة الافتتاحية والفترات المحاسبية: الحالة الفارغة، إخفاء
أزرار الإدارة بلا صلاحية `accounting.opening_balance.manage`/
`accounting.period.manage`، وإقفال فترة فعليًا عبر `api.post`). **30/30**
اختبارًا إجماليًا الآن (11 سابقة + 19 جديدة)، مُتحقَّق فعليًا بالتشغيل.

**Playwright**: `golden-path.spec.ts` مُمدَّد (لا ملف جديد) ليزور فعليًا
كل تبويبات `/reports` (ميزان المراجعة → دفتر الأستاذ → الأرباح والخسائر →
الميزانية العمومية) وتبويب AR في `/receivables-payables` (يتحقق من ظهور
نص الفجوة الهيكلية)، بالإضافة لإنشاء فترة محاسبية فعلية عبر `/accounting`
والتحقق من ظهورها بحالة "مفتوحة". **4/4** Playwright إجماليًا (نفس العدد،
لا ملف جديد)، نُفِّذت ضد Backend حقيقي ونجحت.

لا Model/Migration/Endpoint/صفحة واجهة أمامية جديدة في هذا الـMilestone —
راجع `docs/PROJECT_STATUS.md` "Milestone 5" للتفصيل الكامل.

### Milestone 6 — Weighted-Average Inventory Valuation & COGS

**Backend**: `test/milestone6.e2e-spec.ts` (جديد، 22 اختبارًا): صيغة
المتوسط المرجّح الكاملة (أول شراء، إعادة حساب عند شراء ثانٍ، مثال محسوب
كامل من نهاية لأخرى مُتحقَّق رقميًا مطابق لأمثلة الطلب حرفيًا)، بيع كامل/
جزئي للرصيد وقيمة المخزون الناتجة، دخول مخزون جديد بعد الصفر، مصادر
التكلفة الست (شراء/رصيد افتتاحي/تسوية IN/تسوية OUT/تحويل/بيانات قديمة
بلا رصيد سابق)، قيد COGS/Inventory متوازن، رفض تحديد COGS من العميل
(400 عبر whitelist)، عكس بيع بتكلفة أصلية لا حالية (سيناريو انحراف تكلفة
حقيقي)، بيانات "قديمة" (`unitCost = null`) لا تتعطل، Audit Log لعملية
البيع (`totalCogs`) والشراء، تقارير P&L (COGS/Gross Profit) وBalance
Sheet (قيمة المخزون = الكمية × المتوسط)، نطاق الفروع (متوسط مستقل لكل
مستودع)، RBAC (403)، عزل مستأجرين، Idempotency (لا حركة/COGS مكرَّرة)،
**تزامن حقيقي** (عمليتا شراء متزامنتان حقيقيتان تُنتجان متوسطًا صحيحًا
بغضّ النظر عن ترتيب التنفيذ؛ 10 عمليات بيع متزامنة على 5 وحدات فقط
تُنتج 5 نجاح/5 رفض 409 بلا رصيد سالب)، وثابت مُدين=دائن لكل قيد بيع.
تعديلان على اختبارات موجودة (`milestone1.e2e-spec.ts` P&L،
`phase4.e2e-spec.ts` مجموع مدين/دائن البيع) ليعكسا القيم الصحيحة بعد
إضافة COGS — ليس إخفاءً لتراجع، بل تصحيح توقّعات كانت تفترض عدم وجود
COGS. **161/161** إجماليًا (139 + 22 جديدة).

**Vitest**: `InventoryPage.test.tsx` جديد (4 اختبارات: عرض متوسط
التكلفة/قيمة المخزون، الحالة الفارغة، حقل تكلفة اختياري عند رصيد
افتتاحي/تسوية-زيادة فقط لا عند تسوية-نقصان)، وتمديد `ReportsPage
.test.tsx` (تبويب P&L يعرض COGS وGross Profit). **34/34** إجماليًا
(30 + 4 جديدة).

**Playwright**: `golden-path.spec.ts` مُمدَّد (لا ملف جديد) — يتحقق من
عمودَي متوسط التكلفة/قيمة المخزون في `/inventory` بعد الرصيد الافتتاحي،
ومن ظهور COGS/Gross Profit في تبويب P&L بـ`/reports`. **4/4** إجماليًا
(نفس العدد).

Schema: عمودان جديدان فقط (`stock_levels.average_cost`،
`sale_items.unit_cost`)، Migration واحدة نظيفة، لا drift. راجع
`docs/PROJECT_STATUS.md` "Milestone 6" و`docs/ACCOUNTING.md` "COGS /
تقييم المخزون" للتفصيل الكامل. Docker: BLOCKED BY ENVIRONMENT (daemon
غير متاح في هذه البيئة).

## اختبارات Milestone 7: Merchant Operations & Business Completion — إلزامية

**Backend**: `test/milestone7.e2e-spec.ts` (جديد، 34 اختبارًا): بيع آجل
بالكامل/جزئي (يتطلب عميلًا، يرفض تجاوز الإجمالي بـ400)، تسجيل دفعة على
بيع آجل (قيد Dr نقد/Cr ذمم مدينة، منع دفع زائد بـ409، idempotency، عزل
مستأجرين، رفض على بيع ملغى، RBAC 403، **تزامن حقيقي** بطلبي دفع
متزامنين لا يُنتجان دفعًا زائدًا)، دفعة مورد جزئية (قيد Dr ذمم دائنة/Cr
نقد، رفض تجاوز الرصيد، رفض على أمر لم يُستلَم، RBAC)، مرتجع مبيعات جزئي
(عكس إيراد/ضريبة/COGS نسبيًا بالتكلفة الأصلية، رفض تجاوز الكمية المتاحة
عبر عدة مرتجعات، مرتجعات متعددة متراكمة، سياسة استرداد الذمم أولًا، رفض
على بيع ملغى، RBAC)، مرتجع مشتريات جزئي (عكس ذمم دائنة/مخزون/ضريبة، عدم
تعديل الشراء الأصلي، رفض تجاوز الكمية المستلمة، رفض على أمر لم يُستلَم،
RBAC)، محاسبة تسويات/جرد المخزون (قيد زيادة ونقصان منفصلَين، جرد بفروق
مختلطة يُرحِّل ربحًا وخسارة إجماليَين منفصلَين بلا تقاصّ، تسوية بلا فرق
قيمة لا تُرحِّل شيئًا)، التسوية البنكية/النقدية (حساب الرصيد الدفتري من
القيود المُرحَّلة، RBAC للإنشاء مقابل القراءة)، عزل مستأجرين/IDOR صريح
لكل مسار جديد، **اختبار RLS مباشر** (استعلام SQL عبر Prisma بلا فلتر
`companyId` صريح تحت سياق منشأة أخرى، لكل الجداول الستة الجديدة معًا —
نفس نمط `phase4.e2e-spec.ts` "RLS مباشرة")، وسيناريو تكامل مالي شامل
واحد (شراء→استلام→دفعة جزئية لمورد→بيع آجل→دفعتان→مرتجع مبيعات→مرتجع
مشتريات→تسوية مخزون→جرد، مع التحقق من توازن ميزان المراجعة بعد **كل**
خطوة، وتطابق الميزانية العمومية/تقييم المخزون النهائي). **195/195**
إجماليًا (161 + 34 جديدة).

**Vitest**: `PurchasesPage.test.tsx` جديد (3 اختبارات: إخفاء زر
التفاصيل لأمر غير مُستلَم، عرض الرصيد المستحق وتسجيل دفعة، إخفاء نموذج
المرتجع بلا صلاحية)، تمديد `InvoicesPage.test.tsx` (+3: إخفاء زر
التفاصيل لبيع ملغى، تسجيل دفعة على بيع آجل، إخفاء نموذج المرتجع بلا
صلاحية)، تمديد `AccountingPage.test.tsx` (+3: تبويب التسوية البنكية —
حالة فارغة بلا صلاحية إدارة، عرض تسويات موجودة، إنشاء تسوية جديدة).
تصحيح اختبار موجود واحد (`ReceivablesPayablesPage.test.tsx`) ليعكس نص
الحالة الفارغة الجديد لـAR (لم يعد "لا يدعم البيع الآجل" لأنه أصبح
مدعومًا فعليًا) — ليس إخفاءً لتراجع، بل تحديث توقّع كان صحيحًا قبل هذا
الـMilestone. **43/43** إجماليًا (34 + 9 جديدة).

**Playwright**: `golden-path.spec.ts` مُمدَّد (لا ملف جديد، **4/4** إجماليًا
بنفس العدد) — يضيف: دفعة مورد جزئية عبر تفاصيل أمر الشراء ثم مرتجع
مشتريات كامل، مرتجع مبيعات على البيع النقدي الأصلي، بيع آجل جديد عبر POS
(عميل مُختار بلا أي دفعة) ثم ظهوره في AR ثم تسديده بالكامل عبر تفاصيل
الفاتورة، تسوية مخزون تظهر كقيد `StockAdjustment` في القيود المحاسبية،
وتسوية بنكية جديدة تظهر رصيدها الدفتري المحسوب. نُفِّذت فعليًا ضد الحزمة
الحقيقية الكاملة (تسجيل الدخول الأخير كشف عن ثغرة بيئة حقيقية — دور
`qeedha_auth_lookup` كان بلا صلاحيات `GRANT USAGE ON SCHEMA public`/
`SELECT` في قاعدة بيانات هذه الجلسة تحديدًا رغم توثيق ذلك في
`prisma/manual-sql/001_auth_lookup_role.sql`/`002_...update.sql` —
أُصلِحت بتشغيل السكربتين يدويًا كما توثّق CI، وليست عِلّة في كود
Milestone 7 نفسه).

Schema: 6 جداول جديدة (`supplier_payments, sale_returns,
sale_return_items, purchase_returns, purchase_return_items,
bank_reconciliations`) + عمود واحد على جدول موجود
(`payments.client_reference_id`) + 3 حسابات جديدة في دليل الحسابات (تُزرَع
تلقائيًا للمنشآت الجديدة، وBackfill idempotent لمنشآت موجودة سابقًا)،
Migration واحدة نظيفة، لا drift. راجع `docs/PROJECT_STATUS.md` "Milestone
7" و`docs/ACCOUNTING.md` "Milestone 7" للتفصيل الكامل. Docker: BLOCKED BY
ENVIRONMENT (daemon غير متاح في هذه البيئة، كما في كل مرحلة سابقة).

## اختبارات Milestone 8: SaaS / Subscription & Billing — إلزامية

**Backend**: `test/milestone8.e2e-spec.ts` (جديد، 14 اختبارًا): اشتراك
تجريبي افتراضي عند التسجيل (خطة Professional، 14 يومًا، `/subscriptions
/me` يعكس الحالة/الميزات/الاستخدام)، كتالوج الخطط (`/subscriptions
/plans`)، أحداث Audit لإنشاء الاشتراك وبدء التجربة، **صلاحيات ميزات
مبنية على الخطة رغم RBAC كاملة** (مالك بكل صلاحيات على خطة Starter لا
يزال يُمنَع `403` من Excel Import/AR-AP، بينما POS المشمولة في نفس
الخطة تعمل — يثبت أن المنع خاص بالميزة لا حالة عامة)، ترقية خطة تُعيد
فتح ميزة فورًا، **حدود استخدام تسلسلية ومتزامنة** لثلاثة موارد
(مستخدمون بحد صارم يُرفَض تجاوزه، **طلبان متزامنان حقيقيان**
(`Promise.all`) على آخر مقعد مستخدم متاح — واحد ينجح بالضبط والعدد
النهائي لا يتجاوز الحد أبدًا، فروع بحد الخطة الأساسية، مبيعات شهرية
بحد خطة اختبار مخصَّصة بلا احتساب مضاعف لطلب مكرَّر بنفس
`clientReferenceId`)، دورة حياة تجربة (انتهاء يُقيِّد الطلبات المُغيِّرة
فقط، القراءة و`/auth/me` تبقيان متاحتين، الانتقال يُسجَّل مرة واحدة
فقط في Audit)، إيقاف منشأة (يمنع كل شيء ما عدا مسار الاستعادة/الاطلاع
المُعفى)، وعزل مستأجرين مباشر لجدول `subscriptions` عبر RLS (بنفس نمط
الفحص المباشر المُستخدَم منذ Phase 4/Milestone 7). **209/209** إجماليًا
(195 + 14 جديدة).

**Vitest**: `SubscriptionPage.test.tsx` جديد (4 اختبارات: عرض الخطة
الحالية/حالة التجربة/الميزات/الاستخدام، شريط تنبيه عند التقييد، **تأكيد
عدم وجود أي زر دفع/Checkout** والاعتماد على رسالة "تواصل مع الدعم"
بدلًا منه، بانر خطأ عند فشل الطلب). **47/47** إجماليًا (43 + 4 جديدة).

**Playwright**: `golden-path.spec.ts` مُمدَّد (لا ملف جديد، **4/4**
إجماليًا بنفس العدد) — يضيف قسمًا يزور `/subscription` بعد كل خطوات
الأسرة الذهبية (منتج/مخزون/عميل/مورد/بيع/مصروف/محاسبة) ويتحقق من ظهور
الخطة الاحترافية، حالة "فترة تجريبية"، الأيام المتبقية، ميزة الذمم
المدينة والدائنة، الخطة الأساسية ضمن الكتالوج، ورسالة "تواصل مع الدعم" —
نُفِّذ فعليًا ضد الحزمة الحقيقية الكاملة، وليس Mock. **نفس فجوة بيئة
`qeedha_auth_lookup` المُسجَّلة في تقرير Milestone 7 (`GRANT USAGE`/
`SELECT` مفقود في حاوية هذه الجلسة الجديدة تحديدًا) ظهرت مجددًا وأُصلِحت
بنفس السكربتين الموثَّقين — ليست عِلّة في كود Milestone 8**.

Schema: جدولان جديدان (`plans` بلا RLS، `subscriptions` بـ RLS FORCE)،
Migration واحدة نظيفة، لا drift على أي جدول من المراحل السابقة. راجع
`docs/PROJECT_STATUS.md` "Milestone 8" و`docs/DOMAIN_MODEL.md`
"Milestone 8" للتفصيل الكامل. Docker: BLOCKED BY ENVIRONMENT (نفس القيد
منذ كل مرحلة سابقة).

## Playwright — الآن مجموعة مُلتزَمة (committed) وليست سكربتات مؤقتة

**تصحيح إطار سابق**: أقسام "Browser test (Playwright)" أعلاه (المرحلة
3/4/Milestone 1) وصفت تشغيلًا **يدويًا** لسكربتات مؤقتة (scratchpad) لم
تُحفظ في المستودع. هذا الوصف كان دقيقًا وقتها. **في Milestone 2 هذا لم
يعد الحال**: توجد الآن مجموعة Playwright **مُلتزَمة فعليًا في المستودع**
وقابلة للتكرار من أي شخص، لا سكربتات مؤقتة تُكتب وتُشغَّل يدويًا في كل
مرة.

`frontend/playwright.config.ts`: يفترض أن الـbackend API يعمل ومتاح
مسبقًا (نفس افتراض مجموعة e2e الخلفية — Postgres مُهاجَر ومزروع)، ويبدأ
خادم تطوير الواجهة الأمامية فقط بنفسه عبر `webServer` (`npm run dev`).

**الملفات**:
- `frontend/e2e/helpers.ts`: مساعدات مشتركة (تسجيل منشأة، دخول، خروج،
  توليد معرّف فريد).
- `frontend/e2e/golden-path.spec.ts`: الرحلة الكاملة عبر الواجهة الحقيقية
  ضد الـbackend الحقيقي (لا طلبات مُقلَّدة) — تسجيل → منتج → مخزون (رصيد
  افتتاحي) → عميل → مورد → شراء → استلام → بيع POS → دفعة → فاتورة →
  مصروف → محاسبة (القيود تُظهر البيع/الشراء/المصروف) → تقارير (ميزان
  المراجعة متوازن → دفتر الأستاذ → الأرباح والخسائر → الميزانية العمومية،
  مُمدَّدة في Milestone 5) → الذمم (AR فارغ لعدم وجود بيع آجل بعد في هذه
  اللحظة من السيناريو، AP يُظهر المورد) → دفعة مورد جزئية + مرتجع مشتريات
  (Milestone 7) → مرتجع مبيعات (Milestone 7) → بيع آجل جديد عبر POS ثم
  ظهوره في AR ثم تسديده بالكامل (Milestone 7) → تسوية مخزون تظهر في
  القيود المحاسبية + تسوية بنكية جديدة (Milestone 7) → فترة محاسبية جديدة
  (Milestone 5) → تسجيل خروج → دخول مجددًا.
- `frontend/e2e/tenant-isolation.spec.ts`: تسجيل منشأتين منفصلتين
  والتحقق أن منتجًا أنشأته المنشأة الأولى غير مرئي للثانية عبر الواجهة
  الحقيقية.
- `frontend/e2e/excel-import.spec.ts` (Milestone 3): استيراد منتجات من
  ملف `.xlsx` حقيقي مُلتزَم بالمستودع، عبر التدفق الكامل Upload → Map →
  Preview → Validate → Confirm، بالتحقق من ظهور المنتجَين المستورَدَين
  فعليًا في `/products`.
- `frontend/e2e/einvoice-qr.spec.ts` (Milestone 4): تسجيل منشأة برقم
  ضريبي → منتج → مخزون → بيع POS → التحقق من ظهور رمز QR حقيقي (صورة
  `data:image/png;base64,...`) على الفاتورة في صفحة المبيعات.

**نُفِّذت المجموعات الأربع فعليًا ضد الحزمة الحقيقية الكاملة (backend +
Postgres مُهاجَر ومزروع + frontend) خلال هذه الجلسة ونجحت كاملة (4/4)** —
ليس وصف نيّة، تشغيل متصفح آلي حقيقي وموثَّق.

تشغيل محلي: `cd frontend && npm run test:e2e` (يتطلب backend يعمل ومهاجَر
ومزروع مسبقًا، تمامًا مثل `backend`'s `npm run test:e2e`).

## قاعدة بيانات الاختبار
قاعدة Postgres منفصلة (`qeedha_accounting_test`)، تُصفَّر (migrate reset) قبل
كل تشغيل لمجموعة الاختبارات في CI، ولا تُشارَك مع بيانات التطوير.

## CI (GitHub Actions) — Milestone 2

`.github/workflows/ci.yml` (جديد): ثلاث jobs مستقلة —
1. **`backend`**: حاوية خدمة Postgres، `npm ci`، `prisma generate`،
   `lint`، `tsc --noEmit`، `build`، `prisma migrate deploy`، إنشاء دور
   `qeedha_auth_lookup` عبر سكربتات `manual-sql` الموجودة أصلًا، بذر
   الصلاحيات/الأدوار، ثم مجموعة e2e الخلفية كاملة.
2. **`frontend`**: `npm ci`، `lint`، `build`، `npm run test` (Vitest).
3. **`e2e`**: Postgres خدمة منفصلة، يبني ويشغّل الـbackend الحقيقي، يثبّت
   متصفحات Playwright (`npx playwright install --with-deps chromium`)،
   يشغّل مجموعة Playwright المُلتزَمة ضده، ويرفع تقرير HTML كـartifact
   عند الفشل.

**تنبيه صريح مهم**: مِلفّ الـYAML تحقَّق من صحته النحوية فقط
(`python3 -c "import yaml; yaml.safe_load(...)"` نجح) — **لم يُشغَّل فعليًا
على أي GitHub Actions runner حقيقي على الإطلاق**، لأن بيئة التطوير هذه لا
تملك وصولًا لتشغيله. لا يُدَّعى أنه "ينجح على CI" — هو جاهز وصحيح
نحويًا، والتنفيذ الفعلي غير مُتحقَّق منه. راجع `docs/DEPLOYMENT.md`
"القيود المعروفة".

## معيار قبول كل مرحلة
لا تُعتبر مرحلة مكتملة إلا إذا: (1) الكود يبني بدون أخطاء TypeScript، (2) كل
الاختبارات المذكورة أعلاه خضراء، (3) `PROJECT_STATUS.md` محدَّث.
